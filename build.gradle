plugins {
    id 'com.palantir.git-version' version '0.10.1'
    id 'org.echocat.golang' version '0.1.16'
}

group 'github.com/spirrello/url-test'
version gitVersion()

golang {
    platforms = findProperty('build.platform') ?: 'linux-amd64'

    // dependencies {
    //     deleteAllCachedDependenciesOnClean = true
    //     build 'github.com/sirupsen/logrus'
    //     build 'github.com/blacked/go-zabbix'
    //     build 'github.com/go-resty/resty'
    //     build 'github.com/jasonlvhit/gocron'
    // }

    toolchain {
        cgoEnabled = false
    }
}